# ---- Week 1 (CPU only, install locally now) ----
numpy>=1.26
pandas>=2.0
tables>=3.9          # PyTables: read metr-la.h5 via pandas.read_hdf
pyyaml>=6.0          # config.yaml
matplotlib>=3.7      # results/horizon_gap.png
pytest>=7.4          # tests/

# ---- Week 2+ (models). Installed on Colab / GPU box, not needed for Week 1 ----
# Kept here for a complete record; do NOT install these until the LSTM step.
# torch>=2.1
# torch_geometric>=2.4
#
# NOTE: torch_geometric_temporal is deliberately EXCLUDED (CLAUDE.md §3):
# its one-line METR-LA loader hides the data pipeline we must understand.
